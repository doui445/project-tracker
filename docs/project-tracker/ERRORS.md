# ERRORS — project-tracker

Bug → cause → fix, searchable. Append-only. One entry per significant resolved
bug.

_No entries yet._ Add one whenever a non-trivial bug is diagnosed and fixed:
what broke, the root cause, the fix, and how to recognise it again.
